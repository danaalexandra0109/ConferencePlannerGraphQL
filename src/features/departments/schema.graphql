type Departament {
  id: Int
  name: String
  code: String
  description: String
  employees: Int
}

# type ResponseType {
#   success: Boolean
#   message: String
# }

extend type Query {
  departmentsList: [Departament]
}

extend type Mutation {
  deleteDepartment(id: Int): String
}
